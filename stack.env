# Environment values consumed by the Portainer stack
# Update IMAGE when publishing a new tag to your registry
IMAGE=grovesdigital/pos-gen-lead-visualiser:latest
# Host port to expose the nginx container on
HOST_PORT=7888
# Internal port exposed by nginx inside the container
CONTAINER_PORT=80
